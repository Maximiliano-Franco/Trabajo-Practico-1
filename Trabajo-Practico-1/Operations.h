#include <stdio.h>



/** \brief Permite calcular la division entre 2 numeros.
 *
 * \param float - Primer operando introducido.
 * \param float - Segundo operando introducido.
 * \return float - Devuelve un flotante como resultado.
 *
 */
float CalcularDivision(float, float);



/** \brief Permite calcular el factorial entre 2 numeros.
 *
 * \param int - Primer numero introducido.
 * \return int - Devuelve un entero como resultado.
 *
 */
int CalcularFactorial(int);



/** \brief Permite calcular la multiplicacion entre 2 numeros.
 *
 * \param float - Primer operando introducido.
 * \param float - Segundo operando introducido.
 * \return float - Devuelve un flotante como resultado.
 *
 */
float CalcularMultiplicacion(float, float);



/** \brief Permite calcular la resta entre 2 numeros.
 *
 * \param float - Primer operando introducido.
 * \param float - Segundo operando introducido.
 * \return float - Devuelve un flotante como resultado.
 *
 */
float CalcularResta(float, float);



/** \brief Permite calcular la suma entre 2 numeros.
 *
 * \param float - Primer operando introducido.
 * \param float - Segundo operando introducido.
 * \return float - Devuelve un flotante como resultado.
 *
 */
float CalcularSuma(float, float);
